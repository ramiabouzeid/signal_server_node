# ── MySQL ─────────────────────────────────────────────────────────────────
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=signaluser
DB_PASS=YOUR_DB_PASSWORD_HERE
DB_NAME=signaldb

# ── Security (generate: node -e "require('crypto').randomBytes(32).toString('hex')|console.log") ──
INGEST_SECRET=CHANGE_THIS_INGEST_SECRET
ADMIN_KEY=CHANGE_THIS_ADMIN_KEY

# ── Server ────────────────────────────────────────────────────────────────
PORT=8000
